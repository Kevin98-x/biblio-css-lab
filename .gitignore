# --- JS lab submission ---
# The external stylesheet AND the external JavaScript are both graded now,
# so css/ and js/ are pushed. Only local placeholder assets and noise stay out.

assets/

# editor / OS noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log

# packaging
*.zip
node_modules/
