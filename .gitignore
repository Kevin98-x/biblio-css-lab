# --- Biblio submission: only HTML goes to GitHub ---
# The CSS / JS / assets exist locally so the site looks good while you build,
# but they are NOT pushed. On GitHub Pages the pages render as pure HTML,
# which is exactly what the assignment asks for.

js/
assets/

# editor / OS noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log

# build / packaging
*.zip
node_modules/
